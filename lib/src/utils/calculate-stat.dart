
  // void calculateStats() {
  //   int valueCoding = int.tryParse(inputCodingController.text) ?? 0;
  //   int valueResearch = int.tryParse(inputResearchController.text) ?? 0;
  //   int valueMeeting = int.tryParse(inputMeetingController.text) ?? 0;

  //   totalValue = valueCoding + valueResearch + valueMeeting;

  //   calculatePercentage(int input) {
  //     return totalValue != 0 ? (input / totalValue * 100).round() : 0;
  //   }

  //   setState(() {
  //     codingPercentage = calculatePercentage(valueCoding);
  //     researchPercentage = calculatePercentage(valueResearch);
  //     meetingPercentage = 100 - codingPercentage - researchPercentage;
  //   });
  // }
